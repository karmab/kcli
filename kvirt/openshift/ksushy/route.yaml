apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: ksushy
  name: ksushy
  namespace: kcli-infra
spec:
  host: ksushy.apps.{{ cluster }}.{{ domain }}
  port:
    targetPort: 8000
  to:
    kind: Service
    name: ksushy
    weight: 100
  wildcardPolicy: None
