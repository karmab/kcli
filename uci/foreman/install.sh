export ORG="Karmalabs"
export LOCATION="Madrid"
export PASSWORD="unix1234"
echo `hostname -I` `hostname -s`.cinet `hostname -s` >> /etc/hosts
echo `hostname -I` `hostname -s`.cinet `hostname -s` >> /etc/hostname
